# ReactJS app for french numbers practice
Try it here: https://voyciecho.github.io/reactjs-fr-numbers/

### Install dependencies
`npm install`

`npm install --save-dev gh-pages` # only for deploy

### Build and develop
`npm start`

### Dist
`npm run build`

### Deploy
`npm run deploy`
